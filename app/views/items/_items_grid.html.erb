<% @items.each do |item| %>       
  <% if (item.klass == c && item.type == t) or ((%w[20 21 22].include? item.type.to_s) && item.type == t)  %>     
    <%= link_to(image_tag(item.photo, :size => "70x70", :alt => "#{item.name}"), edit_item_path(item)) if item.photo %>
  <% end %>
<% end %>