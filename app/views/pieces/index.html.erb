<h1>Game Piece Admin</h1>

<table>
  <tr>
    <th>Image</th>
    <th>Name</th>
    <th>Description</th>
    <th>Attack</th>
    <th>Defense</th>
    <th>Movement</th>
    <th>Cost</th>                
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pieces.each do |piece| %>
  <tr>
    <td><%= image_tag piece.image.url(:small) %></td>
    <td><%= piece.name %></td>
    <td><%= piece.description %></td>
    <td><%= piece.attack %></td>
    <td><%= piece.defense %></td>
    <td><%= piece.movement %></td>
    <td><%= piece.cost %></td>                
    <td><%= link_to 'Show', piece %></td>
    <td><%= link_to 'Edit', edit_piece_path(piece) %></td>
    <td><%= link_to 'Destroy', piece, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Piece', new_piece_path %>

<br /><br />

<%= will_paginate @pieces %>
